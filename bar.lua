local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
	-- topmost = "window",
	height = 32,
	margin = 4,
	color = 0x0000000,
	y_offset = 4,
	padding_right = 0,
	padding_left = 0,
})
