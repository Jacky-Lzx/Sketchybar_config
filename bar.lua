local colors = require("colors")

-- Equivalent to the --bar domain
sbar.bar({
	-- topmost = "window",
	height = 26,
	margin = 10,
	color = colors.Catppuccin.Mocha.Base,
	y_offset = 4,
	padding_right = 0,
	padding_left = 0,
})
